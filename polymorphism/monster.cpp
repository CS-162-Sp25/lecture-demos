#include <iostream>
#include "monster.hpp"

monster::monster(int hp) : hp(hp) {}

/*
void monster::attack_player(player& p) {
	// Provide a sort of "default implementation" of this function for
	// all derived classes. If a derived class wants to override it with
	// a more specific definition, it can, but it doesn't have to.
}
*/
